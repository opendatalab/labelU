import jsonschema from './tag-attribute-schema.json';
import JsonSchemaTable from 'components/SchemaTable';

# Tag attribute config schema

<JsonSchemaTable schema={jsonschema} />

## Example

```json
{
  "key": "标签-1",
  "value": "tag-label-1",
  "required": true,
  "type": "enum",
  "options": [
    {
      "key": "标签-1-1",
      "value": "tag-label-1-1"
    }
  ]
}
```
