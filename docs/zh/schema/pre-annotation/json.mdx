import jsonschema from './schema.json';
import imageJsonschema from './image-result-schema.json';
import JsonSchemaTable from 'components/schema-table';

# JSON 预标注格式

JSON 的预标注格式为 LabelU 导出的 json 标注文件，支持图片、视频、音频的标注数据导出。可直接作为预标注导入。

## Schema

导出的 json 文件是一个 数组，每个元素是一个样本的标注信息，包含以下字段：

<JsonSchemaTable schema={jsonschema} />

## 示例

```json
[
  {
    "id": 765, // 从labelu导出的样本 id，可选，不影响作为预标注导入
    "result": "{\"width\": 512, \"height\": 512, \"rotate\": 0, \"annotations\": [{\"toolName\": \"rectTool\", \"result\": [{\"id\": \"9bx61depnkm\", \"x\": 176.8566493955095, \"y\": 189.23661485319516, \"label\": \"label-1\", \"width\": 129.9896373056995, \"height\": 142.36960276338516, \"order\": 1}]}]}",
    "url": "/api/v1/tasks/attachment/upload/271/zh/e875f554-ISIC_0000143_fake_B.png",
    "folder": "/Users/youname/Library/Application Support/labelu/media",
    "fileName": "ISIC_0000143_fake_B.png"
  }
]
```

## 图片 

<JsonSchemaTable schema={imageJsonschema} />

`result` 中的 `annotations` 字段是一个数组，每个元素是一个标注工具的标注结果：

```json
{
  "toolName": "pointTool",
  "result": [
    {
      "x": 134.5060975609757,
      "y": 376.7248475609756,
      "visible": true,
      "id": "pWiUgJIH",
      "order": 3,
      "label": "label-1"
    }
  ]
}
```

图片标注工具所有字段定义如下：

- [标点](/schema/image/point)
- [标线](/schema/image/line)
- [拉框](/schema/image/rect)
- [多边形](/schema/image/polygon)
- [立体框](/schema/image/cuboid)

## 视频

import videoJsonschema from './video-result-schema.json';

<JsonSchemaTable schema={videoJsonschema} />

视频标注工具所有字段定义如下：

- [片断分割](/schema/video/segmentation)
- [时间戳](/schema/video/timestamp)


## 音频

import audioJsonschema from './audio-result-schema.json';

<JsonSchemaTable schema={audioJsonschema} />

音频标注工具所有字段定义如下：

- [片断分割](/schema/audio/segmentation)
- [时间戳](/schema/audio/timestamp)