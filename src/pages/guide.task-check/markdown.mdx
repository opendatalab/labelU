import tasks from './tasks.png';
import task from './task.png';

# 查看任务

1. 在「任务列表」主页点击先前的任务可跳转至相应的标注页面查看到之前的数据标注情况。

<img src={tasks} />

2. 在标注页面的「数据总览」中可以查看到已标注，未标注，跳过，以及总计的对应数量。

<img src={task} />
