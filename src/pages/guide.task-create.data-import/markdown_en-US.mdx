import imageImport from './image-import.png';
import videoImport from './video-import.png';
import audioImport from './audio-import.png';

## Data Import

### Image Import

Click "Upload File" (file types include jpg, png, bmp, gif) or "Upload Folder". After successful upload is displayed on the right side, click "Next" again to save and enter the "Annotation Configuration" page.

**Note：** Currently, data import only supports local data import. It supports file or folder import in two ways. The maximum number of files uploaded at one time is 100, and the size of a single file should not exceed 100MB.

In addition, the image import supports uploading pre-labelled files in jsonl format, refer to [Example](https://opendatalab.github.io/labelU/#/schema/pre-annotation/image)

<img src={imageImport} />

### Video Import

Click "Upload File" (file type includes MP4) or "Upload Folder". After successful upload is displayed on the right side, click "Next" again to save and enter the "Annotation Configuration" page.

In addition, video import supports uploading pre-labelled files in jsonl format, refer to [Example](https://opendatalab.github.io/labelU/#/schema/pre-annotation/video)

<img src={videoImport} />

### Audio Import

Click "Upload File" (file types include MP3, wav, ogg) or "Upload Folder". After successful upload is displayed on the right side, click "Next" again to save and enter the "Annotation Configuration" page.

In addition, audio import supports uploading pre-labelled files in jsonl format, refer to [example](https://opendatalab.github.io/labelU/#/schema/pre-annotation/audio)

<img src={audioImport} />
